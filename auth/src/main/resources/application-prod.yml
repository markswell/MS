spring:
  jpa:
    hibernate:
      ddl-auto: none
  datasource:
    url: ${CADASTRO_DATASOURCE_URL}
    username: ${CADASTRO_DATASOURCE_USER}
    password: ${CADASTRO_DATASOURCE_PASSWORD}

security:
  jwt:
    token:
      secret-key: ${CAHVE_SECRETA_AUTH}
